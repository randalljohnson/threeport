// generated by 'threeport-sdk gen' - do not edit

package handlers

import (
	"errors"
	"fmt"
	v0 "github.com/threeport/threeport/pkg/api/v0"
	gorm "gorm.io/gorm"
)

// ErrUnknownCoreType signals that the given object type is not
// owned by core. Callers should dispatch to the owning module.
var ErrUnknownCoreType = errors.New("object type not owned by core")

// GetCoreObjectNamesByIDs returns id->name for each id of the given
// core object type, or ErrUnknownCoreType if the type isn't a core type.
func GetCoreObjectNamesByIDs(db *gorm.DB, objectType string, ids []uint) (map[uint]string, error) {
	if len(ids) == 0 {
		return map[uint]string{}, nil
	}
	out := make(map[uint]string, len(ids))
	switch objectType {
	case "v0.AwsAccount":
		var rows []v0.AwsAccount
		if err := db.Unscoped().Model(&v0.AwsAccount{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsAccount names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.AwsEksKubernetesRuntimeDefinition":
		var rows []v0.AwsEksKubernetesRuntimeDefinition
		if err := db.Unscoped().Model(&v0.AwsEksKubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.AwsEksKubernetesRuntimeInstance":
		var rows []v0.AwsEksKubernetesRuntimeInstance
		if err := db.Unscoped().Model(&v0.AwsEksKubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ControlPlaneDefinition":
		var rows []v0.ControlPlaneDefinition
		if err := db.Unscoped().Model(&v0.ControlPlaneDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ControlPlaneInstance":
		var rows []v0.ControlPlaneInstance
		if err := db.Unscoped().Model(&v0.ControlPlaneInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ControlPlaneInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.DomainNameDefinition":
		var rows []v0.DomainNameDefinition
		if err := db.Unscoped().Model(&v0.DomainNameDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.DomainNameInstance":
		var rows []v0.DomainNameInstance
		if err := db.Unscoped().Model(&v0.DomainNameInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up DomainNameInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.GatewayDefinition":
		var rows []v0.GatewayDefinition
		if err := db.Unscoped().Model(&v0.GatewayDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.GatewayInstance":
		var rows []v0.GatewayInstance
		if err := db.Unscoped().Model(&v0.GatewayInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up GatewayInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.HelmWorkloadDefinition":
		var rows []v0.HelmWorkloadDefinition
		if err := db.Unscoped().Model(&v0.HelmWorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.HelmWorkloadInstance":
		var rows []v0.HelmWorkloadInstance
		if err := db.Unscoped().Model(&v0.HelmWorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up HelmWorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.KubernetesRuntimeDefinition":
		var rows []v0.KubernetesRuntimeDefinition
		if err := db.Unscoped().Model(&v0.KubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.KubernetesRuntimeInstance":
		var rows []v0.KubernetesRuntimeInstance
		if err := db.Unscoped().Model(&v0.KubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up KubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.LogBackend":
		var rows []v0.LogBackend
		if err := db.Unscoped().Model(&v0.LogBackend{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogBackend names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.LogStorageDefinition":
		var rows []v0.LogStorageDefinition
		if err := db.Unscoped().Model(&v0.LogStorageDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.LogStorageInstance":
		var rows []v0.LogStorageInstance
		if err := db.Unscoped().Model(&v0.LogStorageInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LogStorageInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.LoggingDefinition":
		var rows []v0.LoggingDefinition
		if err := db.Unscoped().Model(&v0.LoggingDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.LoggingInstance":
		var rows []v0.LoggingInstance
		if err := db.Unscoped().Model(&v0.LoggingInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up LoggingInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MachineRuntimeDefinition":
		var rows []v0.MachineRuntimeDefinition
		if err := db.Unscoped().Model(&v0.MachineRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MachineRuntimeInstance":
		var rows []v0.MachineRuntimeInstance
		if err := db.Unscoped().Model(&v0.MachineRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MachineWorkloadDefinition":
		var rows []v0.MachineWorkloadDefinition
		if err := db.Unscoped().Model(&v0.MachineWorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MachineWorkloadInstance":
		var rows []v0.MachineWorkloadInstance
		if err := db.Unscoped().Model(&v0.MachineWorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MachineWorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MetricsDefinition":
		var rows []v0.MetricsDefinition
		if err := db.Unscoped().Model(&v0.MetricsDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.MetricsInstance":
		var rows []v0.MetricsInstance
		if err := db.Unscoped().Model(&v0.MetricsInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up MetricsInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ModuleApi":
		var rows []v0.ModuleApi
		if err := db.Unscoped().Model(&v0.ModuleApi{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleApi names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ModuleController":
		var rows []v0.ModuleController
		if err := db.Unscoped().Model(&v0.ModuleController{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleController names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ModuleObject":
		var rows []v0.ModuleObject
		if err := db.Unscoped().Model(&v0.ModuleObject{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ModuleObject names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ObservabilityDashboardDefinition":
		var rows []v0.ObservabilityDashboardDefinition
		if err := db.Unscoped().Model(&v0.ObservabilityDashboardDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ObservabilityDashboardInstance":
		var rows []v0.ObservabilityDashboardInstance
		if err := db.Unscoped().Model(&v0.ObservabilityDashboardInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityDashboardInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ObservabilityStackDefinition":
		var rows []v0.ObservabilityStackDefinition
		if err := db.Unscoped().Model(&v0.ObservabilityStackDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.ObservabilityStackInstance":
		var rows []v0.ObservabilityStackInstance
		if err := db.Unscoped().Model(&v0.ObservabilityStackInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up ObservabilityStackInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.OciOkeKubernetesRuntimeDefinition":
		var rows []v0.OciOkeKubernetesRuntimeDefinition
		if err := db.Unscoped().Model(&v0.OciOkeKubernetesRuntimeDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.OciOkeKubernetesRuntimeInstance":
		var rows []v0.OciOkeKubernetesRuntimeInstance
		if err := db.Unscoped().Model(&v0.OciOkeKubernetesRuntimeInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.OciProvider":
		var rows []v0.OciProvider
		if err := db.Unscoped().Model(&v0.OciProvider{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up OciProvider names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.Profile":
		var rows []v0.Profile
		if err := db.Unscoped().Model(&v0.Profile{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Profile names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.SecretDefinition":
		var rows []v0.SecretDefinition
		if err := db.Unscoped().Model(&v0.SecretDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.SecretInstance":
		var rows []v0.SecretInstance
		if err := db.Unscoped().Model(&v0.SecretInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up SecretInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.TerraformDefinition":
		var rows []v0.TerraformDefinition
		if err := db.Unscoped().Model(&v0.TerraformDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.TerraformInstance":
		var rows []v0.TerraformInstance
		if err := db.Unscoped().Model(&v0.TerraformInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up TerraformInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.Tier":
		var rows []v0.Tier
		if err := db.Unscoped().Model(&v0.Tier{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up Tier names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.WorkloadDefinition":
		var rows []v0.WorkloadDefinition
		if err := db.Unscoped().Model(&v0.WorkloadDefinition{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up WorkloadDefinition names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	case "v0.WorkloadInstance":
		var rows []v0.WorkloadInstance
		if err := db.Unscoped().Model(&v0.WorkloadInstance{}).Select("id, name").Where("id IN ?", ids).Find(&rows).Error; err != nil {
			return nil, fmt.Errorf("failed to look up WorkloadInstance names: %w", err)
		}
		for _, r := range rows {
			if r.ID != nil && r.Name != nil {
				out[*r.ID] = *r.Name
			}
		}

	default:
		return nil, ErrUnknownCoreType
	}
	return out, nil
}

// GetCoreObjectIDByName returns the ID of the named object of the
// given core object type, or ErrUnknownCoreType if the type isn't a
// core type.
func GetCoreObjectIDByName(db *gorm.DB, objectType string, name string) (uint, error) {
	switch objectType {
	case "v0.AwsAccount":
		var obj v0.AwsAccount
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up AwsAccount by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("AwsAccount %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.AwsEksKubernetesRuntimeDefinition":
		var obj v0.AwsEksKubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("AwsEksKubernetesRuntimeDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.AwsEksKubernetesRuntimeInstance":
		var obj v0.AwsEksKubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up AwsEksKubernetesRuntimeInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("AwsEksKubernetesRuntimeInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ControlPlaneDefinition":
		var obj v0.ControlPlaneDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ControlPlaneDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ControlPlaneDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ControlPlaneInstance":
		var obj v0.ControlPlaneInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ControlPlaneInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ControlPlaneInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.DomainNameDefinition":
		var obj v0.DomainNameDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up DomainNameDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("DomainNameDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.DomainNameInstance":
		var obj v0.DomainNameInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up DomainNameInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("DomainNameInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.GatewayDefinition":
		var obj v0.GatewayDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up GatewayDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("GatewayDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.GatewayInstance":
		var obj v0.GatewayInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up GatewayInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("GatewayInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.HelmWorkloadDefinition":
		var obj v0.HelmWorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up HelmWorkloadDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("HelmWorkloadDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.HelmWorkloadInstance":
		var obj v0.HelmWorkloadInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up HelmWorkloadInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("HelmWorkloadInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.KubernetesRuntimeDefinition":
		var obj v0.KubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up KubernetesRuntimeDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("KubernetesRuntimeDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.KubernetesRuntimeInstance":
		var obj v0.KubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up KubernetesRuntimeInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("KubernetesRuntimeInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.LogBackend":
		var obj v0.LogBackend
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up LogBackend by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("LogBackend %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.LogStorageDefinition":
		var obj v0.LogStorageDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up LogStorageDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("LogStorageDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.LogStorageInstance":
		var obj v0.LogStorageInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up LogStorageInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("LogStorageInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.LoggingDefinition":
		var obj v0.LoggingDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up LoggingDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("LoggingDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.LoggingInstance":
		var obj v0.LoggingInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up LoggingInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("LoggingInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MachineRuntimeDefinition":
		var obj v0.MachineRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MachineRuntimeDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MachineRuntimeDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MachineRuntimeInstance":
		var obj v0.MachineRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MachineRuntimeInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MachineRuntimeInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MachineWorkloadDefinition":
		var obj v0.MachineWorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MachineWorkloadDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MachineWorkloadDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MachineWorkloadInstance":
		var obj v0.MachineWorkloadInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MachineWorkloadInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MachineWorkloadInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MetricsDefinition":
		var obj v0.MetricsDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MetricsDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MetricsDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.MetricsInstance":
		var obj v0.MetricsInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up MetricsInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("MetricsInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ModuleApi":
		var obj v0.ModuleApi
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ModuleApi by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ModuleApi %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ModuleController":
		var obj v0.ModuleController
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ModuleController by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ModuleController %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ModuleObject":
		var obj v0.ModuleObject
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ModuleObject by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ModuleObject %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ObservabilityDashboardDefinition":
		var obj v0.ObservabilityDashboardDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ObservabilityDashboardDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ObservabilityDashboardDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ObservabilityDashboardInstance":
		var obj v0.ObservabilityDashboardInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ObservabilityDashboardInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ObservabilityDashboardInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ObservabilityStackDefinition":
		var obj v0.ObservabilityStackDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ObservabilityStackDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ObservabilityStackDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.ObservabilityStackInstance":
		var obj v0.ObservabilityStackInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up ObservabilityStackInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("ObservabilityStackInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.OciOkeKubernetesRuntimeDefinition":
		var obj v0.OciOkeKubernetesRuntimeDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("OciOkeKubernetesRuntimeDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.OciOkeKubernetesRuntimeInstance":
		var obj v0.OciOkeKubernetesRuntimeInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up OciOkeKubernetesRuntimeInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("OciOkeKubernetesRuntimeInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.OciProvider":
		var obj v0.OciProvider
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up OciProvider by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("OciProvider %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.Profile":
		var obj v0.Profile
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up Profile by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("Profile %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.SecretDefinition":
		var obj v0.SecretDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up SecretDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("SecretDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.SecretInstance":
		var obj v0.SecretInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up SecretInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("SecretInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.TerraformDefinition":
		var obj v0.TerraformDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up TerraformDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("TerraformDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.TerraformInstance":
		var obj v0.TerraformInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up TerraformInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("TerraformInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.Tier":
		var obj v0.Tier
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up Tier by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("Tier %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.WorkloadDefinition":
		var obj v0.WorkloadDefinition
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up WorkloadDefinition by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("WorkloadDefinition %q has nil ID", name)
		}
		return *obj.ID, nil

	case "v0.WorkloadInstance":
		var obj v0.WorkloadInstance
		if err := db.Select("id").Where("name = ?", name).First(&obj).Error; err != nil {
			return 0, fmt.Errorf("failed to look up WorkloadInstance by name: %w", err)
		}
		if obj.ID == nil {
			return 0, fmt.Errorf("WorkloadInstance %q has nil ID", name)
		}
		return *obj.ID, nil

	default:
		return 0, ErrUnknownCoreType
	}
}
