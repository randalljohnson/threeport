// scaffolding generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package v0

import (
	errors "errors"
	"fmt"
	api_v0 "github.com/threeport/threeport/pkg/api/v0"
	client_v0 "github.com/threeport/threeport/pkg/client/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
	"net/http"
)

// GcpSharedNetworkConfig is a config abstraction for the GcpSharedNetwork API object.
// This abstraction allows users to manage API objects with a simplified set of attributes
// and remove the need for users to interract with API object details such as unique IDs
// and foreign keys.
type GcpSharedNetworkConfig struct {
	GcpSharedNetwork GcpSharedNetworkValues
}

// GcpSharedNetworkValues contains all the attributes needed to manage
// the GcpSharedNetwork API object.
type GcpSharedNetworkValues struct {
	// TODO: add config abstraction fields needed for user to manage a GcpSharedNetwork
	Name *string `json:",omitempty"`
	Age  *string `json:",omitempty"`
}

// Get gets gcp shared networks from the Threeport API.
// If the name is set in the GcpSharedNetworkValues, it will return the gcp shared network with that name.
// If the name is not set, it will return all gcp shared networks.
func (g *GcpSharedNetworkConfig) Get(
	apiClient *http.Client,
	apiEndpoint string,
) (*[]GcpSharedNetworkConfig, error) {
	gcpSharedNetworkValues := g.GcpSharedNetwork

	// get API objects
	var gcpSharedNetworks *[]api_v0.GcpSharedNetwork
	switch {
	// if name is provided, get gcp shared network by name
	case gcpSharedNetworkValues.Name != nil:
		gcpSharedNetwork, err := client_v0.GetGcpSharedNetworkByName(apiClient, apiEndpoint, *gcpSharedNetworkValues.Name)
		if err != nil {
			return nil, fmt.Errorf("failed to get gcp shared network with name %s: %w", *gcpSharedNetworkValues.Name, err)
		}
		gcpSharedNetworks = &[]api_v0.GcpSharedNetwork{*gcpSharedNetwork}
	// get all gcp shared networks
	default:
		allGcpSharedNetworks, err := client_v0.GetGcpSharedNetworks(apiClient, apiEndpoint)
		if err != nil {
			return nil, fmt.Errorf("failed to get gcp shared networks from Threeport API: %w", err)
		}
		gcpSharedNetworks = allGcpSharedNetworks
	}

	// assemble config objects from API objects
	var gcpSharedNetworkConfigs []GcpSharedNetworkConfig
	for _, gcpSharedNetwork := range *gcpSharedNetworks {
		// TODO: add config abstraction fields needed for user to manage a GcpSharedNetwork
		gcpSharedNetworkConfig := GcpSharedNetworkConfig{
			GcpSharedNetwork: GcpSharedNetworkValues{
				Age:  util.Ptr(util.GetAgeFormatted(gcpSharedNetwork.CreatedAt)),
				Name: gcpSharedNetwork.Name,
			},
		}
		gcpSharedNetworkConfigs = append(gcpSharedNetworkConfigs, gcpSharedNetworkConfig)
	}

	return &gcpSharedNetworkConfigs, nil
}

// Create creates a gcp shared network in the Threeport API.
func (g *GcpSharedNetworkConfig) Create(
	apiClient *http.Client,
	apiEndpoint string,
) (*GcpSharedNetworkConfig, error) {
	gcpSharedNetworkValues := g.GcpSharedNetwork

	// validate config
	if err := g.Validate(); err != nil {
		return nil, fmt.Errorf("failed to validate values for gcp shared network with name %s: %w", *gcpSharedNetworkValues.Name, err)
	}

	// construct gcp shared network object
	// TODO: add API object fields as needed for GcpSharedNetwork
	gcpSharedNetwork := api_v0.GcpSharedNetwork{
		Instance: api_v0.Instance{Name: gcpSharedNetworkValues.Name},
	}

	// create gcp shared network
	createdGcpSharedNetwork, err := client_v0.CreateGcpSharedNetwork(
		apiClient,
		apiEndpoint,
		&gcpSharedNetwork,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to create gcp shared network in threeport API: %w", err)
	}

	// construct gcp shared network config
	// TODO: add config abstraction fields needed for user to manage a GcpSharedNetwork
	createdGcpSharedNetworkConfig := &GcpSharedNetworkConfig{
		GcpSharedNetwork: GcpSharedNetworkValues{
			Age:  util.Ptr(util.GetAgeFormatted(createdGcpSharedNetwork.CreatedAt)),
			Name: createdGcpSharedNetwork.Name,
		},
	}

	return createdGcpSharedNetworkConfig, nil
}

// Replace updates the entire gcp shared network object in the Threeport API.
// This is a full replacement of all fields in the gcp shared network object.
// This function takes a name parameter to identify the gcp shared network to replace.
// This allows a different name to be provided in the values object for name changes.
func (g *GcpSharedNetworkConfig) Replace(
	apiClient *http.Client,
	apiEndpoint string,
	name string,
) (*GcpSharedNetworkConfig, error) {
	gcpSharedNetworkValues := g.GcpSharedNetwork

	// validate config
	if err := g.Validate(); err != nil {
		return nil, fmt.Errorf("invalid gcp shared network config: %w", err)
	}

	// get existing gcp shared network by name
	existingGcpSharedNetwork, err := client_v0.GetGcpSharedNetworkByName(
		apiClient,
		apiEndpoint,
		name,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to find gcp shared network with name %s: %w", name, err)
	}

	// construct updated gcp shared network object
	// TODO: add API object fields as needed for GcpSharedNetwork
	updatedGcpSharedNetwork := &api_v0.GcpSharedNetwork{
		Common: api_v0.Common{
			ID: existingGcpSharedNetwork.ID,
		},
		Instance: api_v0.Instance{Name: gcpSharedNetworkValues.Name},
	}

	// replace gcp shared network
	replacedGcpSharedNetwork, err := client_v0.ReplaceGcpSharedNetwork(
		apiClient,
		apiEndpoint,
		updatedGcpSharedNetwork,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to replace gcp shared network in threeport API: %w", err)
	}

	// construct updated gcp shared network config
	// TODO: add config abstraction fields needed for user to manage a GcpSharedNetwork
	updatedGcpSharedNetworkConfig := &GcpSharedNetworkConfig{
		GcpSharedNetwork: GcpSharedNetworkValues{
			Age:  util.Ptr(util.GetAgeFormatted(replacedGcpSharedNetwork.CreatedAt)),
			Name: replacedGcpSharedNetwork.Name,
		},
	}

	return updatedGcpSharedNetworkConfig, nil
}

// Delete deletes a gcp shared network from the Threeport API.
func (g *GcpSharedNetworkConfig) Delete(
	apiClient *http.Client,
	apiEndpoint string,
) (*GcpSharedNetworkConfig, error) {
	gcpSharedNetworkValues := g.GcpSharedNetwork

	// get gcp shared network by name
	gcpSharedNetwork, err := client_v0.GetGcpSharedNetworkByName(
		apiClient,
		apiEndpoint,
		*gcpSharedNetworkValues.Name,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to find gcp shared network with name %s: %w", *gcpSharedNetworkValues.Name, err)
	}

	// delete gcp shared network
	deletedGcpSharedNetwork, err := client_v0.DeleteGcpSharedNetwork(
		apiClient,
		apiEndpoint,
		*gcpSharedNetwork.ID,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to delete gcp shared network from Threeport API: %w", err)
	}

	// construct deleted gcp shared network config
	// TODO: add config abstraction fields needed for user to manage a GcpSharedNetwork
	deletedGcpSharedNetworkConfig := &GcpSharedNetworkConfig{
		GcpSharedNetwork: GcpSharedNetworkValues{
			Name: deletedGcpSharedNetwork.Name,
		},
	}

	return deletedGcpSharedNetworkConfig, nil
}

// Validate validates inputs to create gcp shared networks.
func (g *GcpSharedNetworkConfig) Validate() error {
	gcpSharedNetworkValues := g.GcpSharedNetwork
	multiError := util.MultiError{}

	// ensure name is set
	if gcpSharedNetworkValues.Name == nil {
		multiError.AppendError(errors.New("missing required field in config: Name"))
	}

	// TODO: add additional validation as needed

	return multiError.Error()
}
