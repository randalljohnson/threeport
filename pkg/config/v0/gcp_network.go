// scaffolding generated by 'threeport-sdk gen' but will not be regenerated - intended for modification

package v0

import (
	errors "errors"
	"fmt"
	api_v0 "github.com/threeport/threeport/pkg/api/v0"
	client_v0 "github.com/threeport/threeport/pkg/client/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
	"net/http"
)

// GcpNetworkConfig is a config abstraction for the GcpNetwork API object.
// This abstraction allows users to manage API objects with a simplified set of attributes
// and remove the need for users to interract with API object details such as unique IDs
// and foreign keys.
type GcpNetworkConfig struct {
	GcpNetwork GcpNetworkValues
}

// GcpNetworkValues contains all the attributes needed to manage
// the GcpNetwork API object.
type GcpNetworkValues struct {
	// TODO: add config abstraction fields needed for user to manage a GcpNetwork
	Name *string `json:",omitempty"`
	Age  *string `json:",omitempty"`
}

// Get gets gcp networks from the Threeport API.
// If the name is set in the GcpNetworkValues, it will return the gcp network with that name.
// If the name is not set, it will return all gcp networks.
func (g *GcpNetworkConfig) Get(
	apiClient *http.Client,
	apiEndpoint string,
) (*[]GcpNetworkConfig, error) {
	gcpNetworkValues := g.GcpNetwork

	// get API objects
	var gcpNetworks *[]api_v0.GcpNetwork
	switch {
	// if name is provided, get gcp network by name
	case gcpNetworkValues.Name != nil:
		gcpNetwork, err := client_v0.GetGcpNetworkByName(apiClient, apiEndpoint, *gcpNetworkValues.Name)
		if err != nil {
			return nil, fmt.Errorf("failed to get gcp network with name %s: %w", *gcpNetworkValues.Name, err)
		}
		gcpNetworks = &[]api_v0.GcpNetwork{*gcpNetwork}
	// get all gcp networks
	default:
		allGcpNetworks, err := client_v0.GetGcpNetworks(apiClient, apiEndpoint)
		if err != nil {
			return nil, fmt.Errorf("failed to get gcp networks from Threeport API: %w", err)
		}
		gcpNetworks = allGcpNetworks
	}

	// assemble config objects from API objects
	var gcpNetworkConfigs []GcpNetworkConfig
	for _, gcpNetwork := range *gcpNetworks {
		// TODO: add config abstraction fields needed for user to manage a GcpNetwork
		gcpNetworkConfig := GcpNetworkConfig{
			GcpNetwork: GcpNetworkValues{
				Age:  util.Ptr(util.GetAgeFormatted(gcpNetwork.CreatedAt)),
				Name: gcpNetwork.Name,
			},
		}
		gcpNetworkConfigs = append(gcpNetworkConfigs, gcpNetworkConfig)
	}

	return &gcpNetworkConfigs, nil
}

// Create creates a gcp network in the Threeport API.
func (g *GcpNetworkConfig) Create(
	apiClient *http.Client,
	apiEndpoint string,
) (*GcpNetworkConfig, error) {
	gcpNetworkValues := g.GcpNetwork

	// validate config
	if err := g.Validate(); err != nil {
		return nil, fmt.Errorf("failed to validate values for gcp network with name %s: %w", *gcpNetworkValues.Name, err)
	}

	// construct gcp network object
	// TODO: add API object fields as needed for GcpNetwork
	gcpNetwork := api_v0.GcpNetwork{
		Instance: api_v0.Instance{Name: gcpNetworkValues.Name},
	}

	// create gcp network
	createdGcpNetwork, err := client_v0.CreateGcpNetwork(
		apiClient,
		apiEndpoint,
		&gcpNetwork,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to create gcp network in threeport API: %w", err)
	}

	// construct gcp network config
	// TODO: add config abstraction fields needed for user to manage a GcpNetwork
	createdGcpNetworkConfig := &GcpNetworkConfig{
		GcpNetwork: GcpNetworkValues{
			Age:  util.Ptr(util.GetAgeFormatted(createdGcpNetwork.CreatedAt)),
			Name: createdGcpNetwork.Name,
		},
	}

	return createdGcpNetworkConfig, nil
}

// Replace updates the entire gcp network object in the Threeport API.
// This is a full replacement of all fields in the gcp network object.
// This function takes a name parameter to identify the gcp network to replace.
// This allows a different name to be provided in the values object for name changes.
func (g *GcpNetworkConfig) Replace(
	apiClient *http.Client,
	apiEndpoint string,
	name string,
) (*GcpNetworkConfig, error) {
	gcpNetworkValues := g.GcpNetwork

	// validate config
	if err := g.Validate(); err != nil {
		return nil, fmt.Errorf("invalid gcp network config: %w", err)
	}

	// get existing gcp network by name
	existingGcpNetwork, err := client_v0.GetGcpNetworkByName(
		apiClient,
		apiEndpoint,
		name,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to find gcp network with name %s: %w", name, err)
	}

	// construct updated gcp network object
	// TODO: add API object fields as needed for GcpNetwork
	updatedGcpNetwork := &api_v0.GcpNetwork{
		Common: api_v0.Common{
			ID: existingGcpNetwork.ID,
		},
		Instance: api_v0.Instance{Name: gcpNetworkValues.Name},
	}

	// replace gcp network
	replacedGcpNetwork, err := client_v0.ReplaceGcpNetwork(
		apiClient,
		apiEndpoint,
		updatedGcpNetwork,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to replace gcp network in threeport API: %w", err)
	}

	// construct updated gcp network config
	// TODO: add config abstraction fields needed for user to manage a GcpNetwork
	updatedGcpNetworkConfig := &GcpNetworkConfig{
		GcpNetwork: GcpNetworkValues{
			Age:  util.Ptr(util.GetAgeFormatted(replacedGcpNetwork.CreatedAt)),
			Name: replacedGcpNetwork.Name,
		},
	}

	return updatedGcpNetworkConfig, nil
}

// Delete deletes a gcp network from the Threeport API.
func (g *GcpNetworkConfig) Delete(
	apiClient *http.Client,
	apiEndpoint string,
) (*GcpNetworkConfig, error) {
	gcpNetworkValues := g.GcpNetwork

	// get gcp network by name
	gcpNetwork, err := client_v0.GetGcpNetworkByName(
		apiClient,
		apiEndpoint,
		*gcpNetworkValues.Name,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to find gcp network with name %s: %w", *gcpNetworkValues.Name, err)
	}

	// delete gcp network
	deletedGcpNetwork, err := client_v0.DeleteGcpNetwork(
		apiClient,
		apiEndpoint,
		*gcpNetwork.ID,
	)
	if err != nil {
		return nil, fmt.Errorf("failed to delete gcp network from Threeport API: %w", err)
	}

	// construct deleted gcp network config
	// TODO: add config abstraction fields needed for user to manage a GcpNetwork
	deletedGcpNetworkConfig := &GcpNetworkConfig{
		GcpNetwork: GcpNetworkValues{
			Name: deletedGcpNetwork.Name,
		},
	}

	return deletedGcpNetworkConfig, nil
}

// Validate validates inputs to create gcp networks.
func (g *GcpNetworkConfig) Validate() error {
	gcpNetworkValues := g.GcpNetwork
	multiError := util.MultiError{}

	// ensure name is set
	if gcpNetworkValues.Name == nil {
		multiError.AppendError(errors.New("missing required field in config: Name"))
	}

	// TODO: add additional validation as needed

	return multiError.Error()
}
