# generated by 'threeport-sdk gen' but will not be regenerated - can optionally be edited
ARG ARCH=amd64
FROM golang:1.23 as builder
RUN mkdir /build
ADD . /build
WORKDIR /build
RUN CGO_ENABLED=0 GOOS=linux GOARCH=${ARCH} go build -a -o machine-runtime-controller cmd/machine-runtime-controller/main_gen.go

FROM gcr.io/distroless/static:nonroot
COPY --from=builder /build/machine-runtime-controller /
USER 65532:65532
ENTRYPOINT ["/machine-runtime-controller"]
